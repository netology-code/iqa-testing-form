export interface TempTextErr {
    name?: string,
    surname?: string,
    patronymic?: string,
    telephone?: string,
    birthdate?: string,
    passport?: string,
}

export interface TempIsValid {
    name?: boolean,
    surname?: boolean,
    patronymic?: boolean,
    telephone?: boolean,
    birthdate?: boolean,
    passport?: boolean,
}